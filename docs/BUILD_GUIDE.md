# Build Guide

Prerequisites: CMake >= 3.22, Git, Visual Studio 2022 (Desktop C++ workload).

```powershell
.\scripts\setup.ps1
.\scripts\build.ps1
```

Outputs: build/win-x64/Release/*.vst3 and *.exe
